{
  "name": "@repo/plugin-base",
  "version": "1.0.0",
  "description": "Base class for Listening Room plugins with automatic cleanup",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./helpers": "./helpers/index.ts"
  },
  "scripts": {
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write .",
    "test": "vitest run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*"
  },
  "dependencies": {
    "@repo/game-logic": "*",
    "@repo/types": "*",
    "zod": "^4.4.2"
  }
}
