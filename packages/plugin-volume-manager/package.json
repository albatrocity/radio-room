{
  "name": "@repo/plugin-volume-manager",
  "version": "1.0.0",
  "description": "Volume Manager plugin for Listening Room - controls Spotify playback volume via PlaybackController",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./types": "./types.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@repo/plugin-base": "*",
    "@repo/types": "*",
    "zod": "^4.4.2"
  }
}
