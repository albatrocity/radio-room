{
  "name": "@repo/plugin-queue-hygiene",
  "version": "1.0.0",
  "description": "Queue Hygiene plugin for Listening Room - rate limiting and duplicate detection for queue operations",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./types": "./types.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "vitest": "^4.0.13"
  },
  "dependencies": {
    "@repo/plugin-base": "*",
    "@repo/types": "*",
    "zod": "^4.1.13"
  }
}
