{
  "name": "@repo/plugin-quiz-sessions",
  "version": "1.0.0",
  "description": "Quiz Sessions — chat-answered quiz rounds with PvP/PvG modes, scoring, and hot-potato personas",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./types": "./types.ts"
  },
  "scripts": {
    "test": "vitest run --reporter=dot",
    "test:watch": "vitest",
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write ."
  },
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "vitest": "^4.0.13"
  },
  "dependencies": {
    "@repo/game-logic": "*",
    "@repo/plugin-base": "*",
    "@repo/types": "*",
    "@repo/utils": "*",
    "zod": "^4.4.2"
  },
  "peerDependencies": {
    "zod": "^4.4.2"
  }
}
