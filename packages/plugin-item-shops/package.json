{
  "name": "@repo/plugin-item-shops",
  "version": "1.0.0",
  "description": "Item shops with per-user shopping sessions for Listening Room",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/factories": "*",
    "@repo/typescript-config": "*",
    "vitest": "^4.0.13"
  },
  "dependencies": {
    "@repo/plugin-base": "*",
    "@repo/types": "*"
  },
  "peerDependencies": {
    "zod": "^4.4.2"
  }
}
