{
  "name": "@repo/plugin-playlist-democracy",
  "version": "1.0.0",
  "description": "Playlist Democracy plugin for Radio Room - automatically skips tracks that don't meet reaction thresholds",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts",
    "./types": "./types.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "prettier --write . && eslint . --fix",
    "format": "prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "vitest": "^4.0.13"
  },
  "dependencies": {
    "@repo/plugin-base": "*",
    "@repo/types": "*"
  }
}

