/**
 * Icon marking a name or system line the viewer is seeing through with X-Ray
 * (ADR 0149). Matches the X-Ray item definition's icon in
 * `@repo/plugin-item-shops/items/x-ray`; keep the two in step.
 */
export const PIERCE_INDICATOR_ICON = "ScanSearch"
