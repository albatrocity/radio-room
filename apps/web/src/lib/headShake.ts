/**
 * @deprecated Import from `./inventoryItemAnimations` instead.
 * Thin re-export so existing imports keep working during the catalog migration.
 */
export {
  HEAD_SHAKE_DURATION_MS,
  INVENTORY_ITEM_DOM_ATTR,
  playHeadShake,
  shakeInventoryItemById,
} from "./inventoryItemAnimations"
