import React from "react"

import { Room } from "../../types/Room"

type Props = {
  onChange: (settings: Partial<Room>) => void
}

export default function FormJukeboxSettings({ onChange }: Props) {
  return null
}
